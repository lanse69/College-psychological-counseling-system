客户端和服务端通过 TCP 网络协议传输 JSON 数据进行交互。

安全性: 数据库只存在于服务端中，客户端源码甚至不包含 SQL 模块，杜绝了反编译客户端获取数据库权限的风险。

可维护性:
服务端专注数据正确性（如：防止两个学生预约同一医生的同一时间）。
客户端专注用户体验（QML 动画、界面跳转）。

功能实现路径:
管理员报表: 客户端请求 -> 服务端 SQL 聚合 -> 服务端返回 JSON -> 客户端 QML 绘图。
修改预约校验: 客户端只做格式校验（如时间格式） -> 服务端做业务校验（如查询 doctorSchedule 表确认空闲）。
